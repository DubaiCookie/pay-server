# Server Configuration
server.port=8082

# Database Configuration (MariaDB)
spring.datasource.url=jdbc:mariadb://localhost:3379/sql_db
spring.datasource.username=root
spring.datasource.password=SqlDba-1
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=pay-server-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Toss Payment API Configuration
toss.payment.api.url=https://api.tosspayments.com/v1
toss.payment.api.secret-key=test_sk_ORzdMaqN3wKQvgJO9bPP35AkYXQG
# Note: Client Key는 프론트엔드에서 사용 (결제창 호출 시)
toss.payment.api.client-key=test_ck_YOUR_CLIENT_KEY_HERE

# Logging
logging.level.com.payserver=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.kafka=INFO
